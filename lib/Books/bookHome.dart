import 'package:flutter/material.dart';

class BookHome extends StatelessWidget {
  static const String id = 'BookHome';
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Books and notes'),
      ),
    );
  }
}
