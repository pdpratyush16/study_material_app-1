import 'package:flutter/material.dart';

class PracticeHome extends StatelessWidget {
  static const String id = 'PracticeHome';
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Practice questions here'),
      ),
    );
  }
}
