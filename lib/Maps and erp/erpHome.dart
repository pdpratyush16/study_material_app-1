import 'package:flutter/material.dart';

class ErpHome extends StatelessWidget {
  static const String id = 'ErpHome';
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Erp here'),
      ),
    );
  }
}
